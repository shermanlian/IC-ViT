chammi:
  name: CHAMMI
  args:
    csv_path: "/morphem70k_v2.csv"
    root_dir: "/CHAMMI"
    split: train
    channels:
      - 0
      - 1
      - 2
      - 3
      - 4

  # training data loader configuration
  loader:
    num_workers: 32
    batch_size: 32
    pin_memory: True
    drop_last: False
    shuffle: True

  output_dir: null

